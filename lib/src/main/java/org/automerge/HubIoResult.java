package org.automerge;

/**
 * Represents the result types for Hub IO operations in the samod protocol.
 */
public enum HubIoResult {
    /**
     * Indicates a successful send operation.
     */
    SEND,

    /**
     * Indicates a disconnect operation.
     */
    DISCONNECT,
}
